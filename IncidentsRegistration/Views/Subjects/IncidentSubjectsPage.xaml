<Page x:Class="IncidentsRegistration.Views.IncidentSubjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IncidentsRegistration.Views"
      Background="Transparent"
      Title="Участники инцидента">

    <Page.Resources>
        <Style x:Key="IncidentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="SelectionBorder" 
                                Background="Transparent" 
                                BorderBrush="Transparent" 
                                BorderThickness="3" 
                                Margin="0,0,0,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectionBorder" Property="BorderBrush" 
                                        Value="{StaticResource SecondaryYellow}"/>
                                <Setter TargetName="SelectionBorder" Property="Background" Value="#15FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>

        <Grid Margin="24">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Border Background="{StaticResource SecondaryYellow}" 
                            Width="8" Height="30" Margin="0,0,15,0"/>
                    <TextBlock Text="Участники по делам" FontSize="28" 
                               FontWeight="Bold" Foreground="Black"/>
                </StackPanel>
            </StackPanel>

            <ListBox Grid.Row="2" 
                     ItemsSource="{Binding Incidents}"
                     SelectedItem="{Binding SelectedIncident}"
                     Background="Transparent" 
                     BorderThickness="0" 
                     HorizontalContentAlignment="Stretch"
                     ItemContainerStyle="{StaticResource IncidentItemStyle}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SecondaryBlue}" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                BorderThickness="2" 
                                Padding="18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <Border Background="{StaticResource SecondaryYellow}" Padding="10,4">
                                        <TextBlock Text="{Binding RegistrationTime, StringFormat=\{0:HH:mm\}}" 
                                                   FontWeight="Bold" Foreground="Black"/>
                                    </Border>
                                    <TextBlock Text="{Binding RegistrationDate, StringFormat=\{0:dd.MM.yy\}}" 
                                               FontSize="10" 
                                               HorizontalAlignment="Center" Margin="0,4,0,0" Foreground="Black"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="20,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TypeOfIncident}" FontSize="16" 
                                               FontWeight="Bold" Foreground="Black"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="УЧАСТНИКИ: " FontSize="10" FontWeight="Bold" Foreground="Black" Opacity="0.7"/>
                                        <ItemsControl ItemsSource="{Binding SubjectRoles}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Foreground="Black" FontSize="11" Margin="0,0,5,0">
                                                        <Run Text="{Binding RoleName, StringFormat='[{0}]'}" FontWeight="Bold"/>
                                                        <Run Text="{Binding IdSubjectNavigation.LastName}"/>;
                                                    </TextBlock>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Column="2" Background="{StaticResource PrimaryPink}" 
                                        Width="40" Height="4" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource PrimaryPink}"
                           Background="{StaticResource SecondaryYellow}"
                           FontSize="14"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

            <Border Grid.Row="3" Background="{StaticResource PrimaryGreen}" 
                    BorderBrush="{StaticResource PrimaryPink}" 
                    BorderThickness="2" Padding="12" 
                    Margin="0,15,0,0">
                <Grid>
                    <TextBlock Text="Управление составом участников" 
                               VerticalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="ПОДРОБНЕЕ" 
                                Command="{Binding ShowSubjectDetailsCommand}" 
                                Width="150" Height="40" Margin="5,0"
                                Background="{StaticResource SecondaryBlue}" 
                                Foreground="Black" BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>

                        <Button Content="ДОБАВИТЬ" 
                                Command="{Binding AddParticipantCommand}" 
                                Width="150" Height="40" Margin="5,0"
                                Background="{StaticResource SecondaryYellow}" 
                                Foreground="Black" BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>