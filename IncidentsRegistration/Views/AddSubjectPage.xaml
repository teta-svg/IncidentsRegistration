<Page x:Class="IncidentsRegistration.Views.AddSubjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Background="#F0F2F5" Title="Регистрация участника">

    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8B959E"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="ЛИЧНЫЙ СОСТАВ ДЕЛА" FontSize="11" FontWeight="Bold" Foreground="#8B959E"/>
            <TextBlock Text="Регистрация участника" FontSize="28" FontWeight="SemiBold" Foreground="#2D3436"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="White" CornerRadius="20" Padding="25" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.06"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Background="{StaticResource PrimaryGreen}" CornerRadius="10" Padding="12,5" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="ПЕРСОНАЛЬНАЯ ИНФОРМАЦИЯ" Foreground="#2D4A3E" FontWeight="Bold" FontSize="10"/>
                        </Border>

                        <TextBlock Text="Роль в инциденте" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Roles}" 
                                  SelectedItem="{Binding SelectedRole}" Height="40">
                            <ComboBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </ComboBox.Resources>
                        </ComboBox>

                        <TextBlock Text="Фамилия" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NewSubject.LastName}" Height="40"/>

                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Имя" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding NewSubject.FirstName}" Height="40"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Отчество" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding NewSubject.Patronymic}" Height="40"/>
                            </StackPanel>
                        </UniformGrid>

                        <TextBlock Text="Количество судимостей" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NewSubject.NumberOfConvictions}" Height="40" Width="100" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="20" Padding="25" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.06"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Background="{StaticResource SecondaryBlue}" CornerRadius="10" Padding="12,5" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="МЕСТО ЖИТЕЛЬСТВА" Foreground="#4A90E2" FontWeight="Bold" FontSize="10"/>
                        </Border>

                        <TextBlock Text="Населенный пункт (Город/Поселок)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NewSubject.Settlement}" Height="40"/>

                        <TextBlock Text="Улица" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NewSubject.Street}" Height="40"/>

                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Дом" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding NewSubject.House}" Height="40"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Квартира" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding NewSubject.Room}" Height="40"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" 
                Content="ЗАРЕГИСТРИРОВАТЬ И ПРИВЯЗАТЬ" 
                Command="{Binding SaveCommand}" 
                Background="{StaticResource PrimaryGreen}" Foreground="#2D4A3E" 
                FontWeight="Bold" Height="50" Width="300" HorizontalAlignment="Right">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="15"/>
                </Style>
            </Button.Resources>
            <Button.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.2"/>
            </Button.Effect>
        </Button>
    </Grid>
</Page>