<Page x:Class="IncidentsRegistration.Views.IncidentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      Title="Регистрация инцидентов">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="IncidentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="SelectionBorder" 
                                Background="Transparent" 
                                BorderBrush="Transparent" 
                                BorderThickness="3" 
                                Margin="0,0,0,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectionBorder" 
                                        Property="BorderBrush" 
                                        Value="{StaticResource SecondaryYellow}"/>
                                <Setter TargetName="SelectionBorder" 
                                        Property="Background" 
                                        Value="#15FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="GroupName" Value="SortGroup"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd"
                                Background="{StaticResource SecondaryYellow}"
                                BorderBrush="{StaticResource PrimaryPink}"
                                BorderThickness="2"
                                Padding="14,6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" 
                                        Property="Background" 
                                        Value="{StaticResource SecondaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Border Background="{StaticResource PrimaryPink}" 
                        Width="8" Height="30" 
                        Margin="0,0,15,0"/>
                <TextBlock Text="Список инцидентов" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="Black"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,20,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Сортировать по:" 
                               VerticalAlignment="Center" 
                               Foreground="Black" 
                               Margin="0,0,12,0" 
                               FontWeight="Bold" 
                               FontSize="12"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="Времени" 
                                     Command="{Binding SortByCommand}"
                                     CommandParameter="RegistrationTime"
                                     Foreground="Black" 
                                     FontWeight="Medium" 
                                     Margin="0,0,10,0"/>
                        <RadioButton Content="Типу" 
                                     Command="{Binding SortByCommand}"
                                     CommandParameter="TypeOfIncident"
                                     Foreground="Black" 
                                     FontWeight="Medium" 
                                     Margin="0,0,10,0"/>
                        <RadioButton Content="Группе" 
                                     Command="{Binding SortByCommand}"
                                     CommandParameter="IdResponseTeamNavigation.NameTeam"
                                     Foreground="Black" 
                                     FontWeight="Medium"
                                     Visibility="{Binding CanGroup, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" 
                        Background="White" 
                        BorderThickness="2" 
                        BorderBrush="{StaticResource PrimaryPink}" 
                        Padding="10,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0,10,0" Foreground="Black"/>
                        <TextBox x:Name="SearchBox"
                                 Grid.Column="1"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalAlignment="Center"
                                 Foreground="Black"
                                 FontWeight="Medium"/>
                    </Grid>
                </Border>
            </Grid>

            <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource PrimaryPink}"
                           Background="{StaticResource SecondaryYellow}"
                           FontSize="14"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

            <ListBox Grid.Row="2"
                     ItemsSource="{Binding Incidents}"
                     SelectedItem="{Binding SelectedIncident}"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource IncidentItemStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SecondaryBlue}"
                                BorderBrush="{StaticResource PrimaryPink}"
                                BorderThickness="2"
                                Padding="18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <Border Background="{StaticResource SecondaryYellow}" Padding="10,4">
                                        <TextBlock Text="{Binding RegistrationTime, StringFormat=\{0:HH:mm\}}" 
                                                   Foreground="Black" 
                                                   FontWeight="Bold"/>
                                    </Border>
                                    <TextBlock Text="{Binding RegistrationDate, StringFormat=\{0:dd.MM.yy\}}" 
                                               Foreground="Black" FontSize="10" 
                                               HorizontalAlignment="Center" 
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="20,0">
                                    <TextBlock Text="{Binding TypeOfIncident}" 
                                               FontWeight="Bold" 
                                               Foreground="Black" 
                                               FontSize="16"/>
                                    <TextBlock Text="{Binding IdResponseTeamNavigation.NameTeam}" 
                                               Foreground="Black" 
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2" 
                                           Text="{Binding Decision.DecisionType}" 
                                           VerticalAlignment="Center" 
                                           Foreground="Black" 
                                           FontWeight="SemiBold"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="3" Background="{StaticResource PrimaryGreen}" 
                    BorderBrush="{StaticResource PrimaryPink}" 
                    BorderThickness="2" 
                    Padding="12" 
                    Margin="0,15,0,0">
                <Grid>
                    <TextBlock Text="Управление записями" VerticalAlignment="Center" Foreground="Black" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="ПОДРОБНЕЕ" 
                                Command="{Binding DetailsCommand}" 
                                Background="{StaticResource SecondaryBlue}" 
                                Foreground="Black" 
                                Width="110" 
                                Height="40" 
                                Margin="5,0" 
                                BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>
                        <Button Content="ДОБАВИТЬ" 
                                Command="{Binding AddCommand}" 
                                Background="{StaticResource SecondaryYellow}" 
                                Foreground="Black" 
                                Width="110" 
                                Height="40" 
                                Margin="5,0" 
                                BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>
                        <Button Content="ОБНОВИТЬ" 
                                Command="{Binding UpdateCommand}" 
                                Background="{StaticResource PrimaryGreen}" 
                                Foreground="Black" 
                                Width="110" 
                                Height="40" 
                                Margin="5,0" 
                                BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>
                        <Button Content="УДАЛИТЬ" 
                                Command="{Binding DeleteCommand}"
                                Visibility="{Binding CanDelete, Converter={StaticResource BoolToVis}}"
                                Background="{StaticResource PrimaryPink}" 
                                Foreground="Black" 
                                Width="110" 
                                Height="40" 
                                Margin="5,0" 
                                BorderThickness="2" 
                                BorderBrush="{StaticResource PrimaryPink}" 
                                FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>